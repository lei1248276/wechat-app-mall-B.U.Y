<view class="indicator">
  <view class="line"
        style="background: {{ i === index ? '#666' : '#EBEBEB' }};width: {{ w }};height: {{ h }}"
        wx:for="{{ count }}"
        wx:key="index"
        bind:tap="onClick"
        data-index="{{ index }}">
  </view>
</view>